#ifndef MAIN_H
#define MAIN_H
#include <stdlib.h>
#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>

int putchar_(char c);
int _printf(const char *format, ...);
int printf_d_i(const char *format, ...);

#endif
